syntax = "proto3";

package flowpipe.rpc.v1;

service FlowPipeService {
  rpc Run(RunRequest) returns (RunResponse);
}

message RunRequest {
  string payload = 1;
}

message RunResponse {
  string payload = 1;
  string status = 2;
  string processed_by = 3;
}
